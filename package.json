{
  "name": "chaloyaar",
  "version": "1.0.0",
  "private": true,
  "description": "ChaloYaar — time-budget-first trips for Indian travellers",
  "scripts": {
    "check": "node --check app.js && node --check data.js && node --check sw.js && node --check test/smoke.js && node --check test/schema.js && node test/schema.js",
    "test": "node test/schema.js && node test/smoke.js",
    "icons": "node scripts/make-icons.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "pngjs": "^7.0.0"
  }
}
